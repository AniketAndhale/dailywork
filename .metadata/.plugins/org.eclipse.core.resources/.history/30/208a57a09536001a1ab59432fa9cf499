package aniket12;

import java.io.FileInputStream;
import java.io.FileOutputStream;
import java.io.ObjectInputStream;
import java.io.ObjectOutputStream;

public class SerializationDe {
	public static object deserialization(int path)
	{
		FileInputStream fin =new FileInputStream(filename);
		BufferedInputstream bin=new BufferedInputstream(fin);
		ObjectInputStream oin =new ObjectInputStream(bin);
		Object obj =oin.readObject();
		return obj;
		
	}
	public static void serializable (object obj,String filename) {
		FileOutputStream fout=new FileOutputStream(filename);
		BufferedOutputStream bout=new BufferedOutputStream(fout);
		ObjectOutputStream out=new ObjectOutputStream(bout);
		out.close();
		bout.close();
		fout.close();
	}
public static void main(String[] args) {
	Employee employee =new Employee1(111,"sai","kolhapur",22,55885688L,5284.00);
	serialize(employee,"serial.txt");
	
}
}
